import type { Certification } from "./types";

// Intentionally empty — no certification, insurance or partner logo is
// invented. Add entries here once real, verifiable assets are supplied.
export const certifications: Certification[] = [];

export const trustNumbers = {
  installationsCount: null as number | null,
  yearsActive: null as number | null,
  interventionZones: null as string[] | null,
};
